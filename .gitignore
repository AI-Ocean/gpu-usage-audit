# 작업 메모/도구 (절대 commit 금지)
/.claude/

# 빌드 산출물
/dist/
# `go build` 가 root 에 떨어뜨리는 모듈명 바이너리.
# leading slash 로 root 만 — go-source 트리 안 같은 이름은 안 막음.
/gpu-usage-audit

# 로컬 데이터베이스
*.db
*.db-journal
*.db-wal
*.db-shm

# OS 부산물
.DS_Store
Thumbs.db

# 에디터/IDE
.idea/
.vscode/
*.swp
*.swo

# Go
*.test
*.out
coverage.txt
